# WBS 4.1 卷宗审阅层补齐交接

## 本轮目标

1. 处理 `#44 / 4189911724` 的晚到关键评论。
2. 把 `BaseLab` 卷宗独立验证页从“内部操作验证台”补成“可审阅说明页 + 可操作验证页”。
3. 保持范围仍在 `WBS 4.1.2 / 4.1.3 / 4.1.4`，不提前进入 `WBS 4.1.5`。

## 本轮判断

1. 评论 `4189911724` 之前未被正式同步到本地计划与实现。
2. 这条评论要求的核心不是多做几个交互，而是补出：
   - 设计思路入口
   - 功能/页面清单
   - 对象类型覆盖说明
   - 典型链接关系说明
   - 样例数据格式规范入口
3. 因此本轮优先补“审阅层”，而不是直接扩写主工作台功能。

## 本轮修改

1. 在 `apps/base-lab/src/components/DossierAbilityLab.tsx` 中引入三层切换：
   - `设计说明`
   - `样例规范`
   - `工作面验证`
2. 默认进入 `设计说明`，不再一打开就直接落到卷宗操作区。
3. 新增 `apps/base-lab/src/components/DossierReviewDeck.tsx`，承载：
   - 卷宗设计思路
   - 页面清单
   - 对象类型覆盖
   - 典型链接关系
   - 样例规范页
4. 新增 `apps/base-lab/src/lib/dossier-review.ts`，集中维护审阅层所需说明数据和 JSON 样例片段。
5. 更新 `apps/base-lab/src/App.tsx` 中 `WBS 4.1` 卡片摘要，使入口文案反映“审阅 + 验证”双层结构。
6. 更新 `apps/base-lab/src/styles.css`，补充审阅层卡片布局、样例 JSON 展示和与验证层的切换样式。
7. 更新测试：
   - `apps/base-lab/src/components/DossierAbilityLab.test.tsx`
   - `apps/base-lab/src/App.test.tsx`
8. 更新本地设计说明、研制计划、自测报告和验收清单，使它们明确承认卷宗独立验证已包含审阅层。

## 关键结果

1. 现在用户打开 `WBS 4.1 卷宗独立验证` 后，会先看到 `卷宗设计思路`。
2. 用户可直接切到 `样例数据格式规范`，并查看：
   - `fixtures/projects/sample-novel/manifest.json`
   - `fixtures/projects/sample-novel/objects/characters.json`
   - `fixtures/projects/sample-novel/objects/relations.json`
   - `fixtures/projects/sample-novel/views/saved-filters.json`
3. 用户仍可切到 `工作面验证`，继续执行筛选、回查、审校和检查器联动验证。

## 验证结果

1. `npm run test --workspace @novelstory/base-lab -- src/components/DossierAbilityLab.test.tsx src/App.test.tsx`
   - 结果：通过，`13 / 13`
2. `npm run test --workspace @novelstory/base-lab`
   - 结果：通过，`23 / 23`
3. `npm run typecheck --workspace @novelstory/base-lab`
   - 结果：通过，退出码为 `0`
4. `npm run build --workspace @novelstory/base-lab`
   - 结果：通过，成功生成 `dist/` 产物

## 当前状态

1. `WBS 4.1.2 / 4.1.3 / 4.1.4` 仍保持 `待人工验收`。
2. 验收清单已补出新的“审阅层补充人工验收项”，当前尚未人工勾选。
3. `WBS 4.1.5` 仍未开始。
