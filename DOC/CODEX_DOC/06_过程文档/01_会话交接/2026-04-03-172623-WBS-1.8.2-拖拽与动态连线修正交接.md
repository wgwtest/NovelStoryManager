# WBS 1.8.2 拖拽与动态连线修正交接

## 本轮目标

- 响应 `#41` 的人工验收意见，修正 `BaseLab` 实验页的拖拽漂移问题，并补齐端口动态连线能力

## 本轮完成

1. `packages/view-core/` 新增“按原始 offset 重算平移”的能力，并补齐对应测试
2. `apps/base-lab/src/lib/base-selection-lab.ts` 抽出实验页的节点、端口、边和动态连线状态逻辑
3. `apps/base-lab/src/components/BaseSelectionLab.tsx` 已补入：
   - 稳定画布平移
   - 输出端口起拖
   - 动态连线预览
   - 输入端口落点连接
   - `Canvas` 端口命中与连线创建
4. `apps/base-lab/src/components/BaseSelectionLab.test.tsx` 已补界面级回归测试
5. `WBS 1.8.2` 与 `WBS 3.1` 相关计划、设计说明、验收清单已同步更新

## 当前状态

1. `WBS 1.8.2` 已完成“拖拽与动态连线修正”自测，当前状态保持为 `待人工验收`
2. `WBS 3.1` 的 `BaseLab` 验证页现在更接近真实关系图基座观察面

## 下一步建议

1. 人工重点验证三块画布的拖拽稳定性是否达到可接受程度
2. 人工验证三块画布的动态连线是否足以支撑后续 `WBS 5.1` 蓝图节点实验
3. 若通过，本轮可在 `#41` 上转入验收结论同步；若仍有意见，继续在 `BaseLab` 内修正，不回退主工作台结构
