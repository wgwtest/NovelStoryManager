# WBS 2.2 规划重排与状态同步交接

## 本轮完成

1. 将 `WBS 2.1` 本地契约状态改为 `已验收`，并新增验收记录与验收结论文档
2. 将 GitHub issue `#2` 关闭，并将 Project 对应条目标记为 `Done`
3. 将误关闭的 GitHub issue `#3` 重新打开，并将其标题改为 `WBS 2.2 数据规范与导入导出规范`
4. 将 GitHub Project 中 `#3` 的 `Status` 调整为 `In Progress`
5. 新增 `WBS 2.2` 规范正文：`DOC/CODEX_DOC/04_研发文档/01_数据规范/2026-04-02-WBS-2.2-项目数据规范与导入导出规范.md`
6. 新增 `WBS 2.2` 实施计划：`DOC/CODEX_DOC/03_研制计划/11-WBS-2.2-数据规范与导入导出规范-研制计划.md`
7. 更新项目策略和全局策略，补充“叶子节点命名直白、成果物落到明确路径、数据规范类任务必须包含正反样例和往返测试”的规则

## 当前状态

1. 当前活动链路：`WBS 0 -> WBS 2 -> WBS 2.2`
2. 当前活动节点：`WBS 2.2 数据规范与导入导出规范`
3. GitHub 当前状态：
   - `#2`：`CLOSED / Done`
   - `#3`：`OPEN / In Progress`
   - `#10`：`OPEN / In Progress`

## 下步建议

1. 先由用户人工评阅 `WBS 2.2` 规范正文和实施计划
2. 如用户确认规划，直接开始补 `fixtures/import-export/`、schema 校验和 service 往返测试
3. 实现时严格按计划文档中的测试矩阵推进，不再回到抽象验收描述
