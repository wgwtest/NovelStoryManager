# WBS 1.7.2 与 WBS 4 任务树同步交接

## 本轮完成

1. 补回 `DOC/CODEX_DOC/04_研发文档/03_原型图/WBS-1.7.2/WBS-1.7.2-原型稿执行说明.md`
2. 更新 `DOC/CODEX_DOC/00-本地工程策略映射.md` 的当前活动链路与“多视图”口径
3. 更新 `DOC/CODEX_DOC/README.md` 的当前状态与研发文档目录说明
4. 将 GitHub issue `#43` 状态调整为 `待人工验收`，并回写最新评论 `4188222166`
5. 将 GitHub issue `#8` 当前主线更新为 `WBS 0 -> WBS 1 -> WBS 1.7 -> WBS 1.7.2`
6. 重排 GitHub WBS 树：
   - `#12 -> #44 -> #5/#25/#26/#27`
   - `#12 -> #13 -> #6/#28/#29/#30`
   - `#12 -> #14 -> #7/#31/#32/#33`
7. 向 GitHub Project 补入 `#44`、`#32`、`#33` 三个条目，并写入 `Status / Work Type / Start Date`
8. 新增本轮自测报告 `2026-04-05-131123-WBS-1.7.2与WBS4任务树同步自测报告.md`
9. 修正 `scripts/generate-wbs-1.7.2-prototypes.mjs` 的目录清理策略，避免误删 `WBS-1.7.2` 根目录下的执行说明文档
10. 为上述回归补充 `scripts/generate-wbs-1.7.2-prototypes.test.mjs` 失败测试并转绿

## 核验结果

1. `#43` 与 `#8` 的正文已按最新评论后的口径更新
2. `#12 / #44 / #13 / #14` 的子树顺序已核对正确
3. 项目条目 `PVTI_lAHOD9ycyM4BTOqkzgpInEY`、`PVTI_lAHOD9ycyM4BTOqkzgpBXAg`、`PVTI_lAHOD9ycyM4BTOqkzgpBXUU` 已确认存在
4. 原型脚本测试、原稿生成、PNG 导出都已通过
5. 重新生成原型后，`WBS-1.7.2-原型稿执行说明.md` 仍保留在根目录

## 剩余事项

1. 当前节点仍待人工验收，不应将 `#43` 或 `WBS 1.7.2` 提前改为完成态
2. GitHub CLI 的 `gh project item-list` 默认输出在本轮排查中不适合直接用来确认新增条目，后续优先用具体 `ProjectV2Item id` 查询
3. 下一轮如继续推进实现，应从 `WBS 4` 视图设计分支进入，不再回到旧的 `WBS 5 / WBS 6` 命名
