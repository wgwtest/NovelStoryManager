# WBS 4.1 卷宗独立验证补强交接

## 本轮目标

1. 拉取远端 issue 评论与本地验收清单意见，确认真实反馈源。
2. 针对 `【你设置的验收项目已经通过，但是我觉得内容还缺很多】` 做定向补强。
3. 保持范围在 `WBS 4.1.2 / 4.1.3 / 4.1.4` 的卷宗独立验证，不提前进入 `WBS 4.1.5`。

## 反馈结论

1. 远端 `#25 / #26 / #27` 未发现新的业务性评阅意见，只有既有状态同步评论。
2. 本轮唯一新增有效意见来自本地验收清单，核心含义是“当前验收覆盖密度不足”。
3. 除了补验收脚本外，实际代码中还暴露出一个真实交互缺口：关键字筛选后中央卷宗不会自动切到首个匹配对象。

## 本轮修改

1. 在 `apps/base-lab/src/components/DossierAbilityLab.tsx` 中修正关键字筛选后的激活对象选择逻辑，使中央卷宗跟随首个可见匹配对象。
2. 将引用回查按钮文案统一为 `回查 <对象名> 卷宗`，同时补齐对应的可访问性命名。
3. 为当前激活对象补充动态补录提示，选中审校队列对象后可在正文区直接看到 `待补录字段`。
4. 在 `apps/base-lab/src/components/DossierAbilityLab.test.tsx` 中补强以下自动化验证：
   - 关键字筛选自动切换
   - 引用回查返回
   - 审校队列跳转后的补录提示
   - 检查器只读字段分组
5. 更新以下本地文档：
   - `DOC/CODEX_DOC/05_测试文档/02_验收清单/2026-04-06-014348-WBS-4.1.2-4.1.3-4.1.4-卷宗独立验证验收清单.md`
   - `DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-04-06-014348-WBS-4.1.2-4.1.3-4.1.4-卷宗独立验证自测报告.md`
   - `DOC/CODEX_DOC/03_研制计划/19-WBS-4.1.2-卷宗能力验证-研制计划.md`
   - `DOC/CODEX_DOC/03_研制计划/20-WBS-4.1.3-卷宗功能验证-研制计划.md`
   - `DOC/CODEX_DOC/03_研制计划/21-WBS-4.1.4-卷宗样式验证-研制计划.md`

## 验证结果

1. `npm run test --workspace @novelstory/base-lab -- src/components/DossierAbilityLab.test.tsx`
   - 结果：通过，`8 / 8` 项通过
2. `npm run test --workspace @novelstory/base-lab`
   - 结果：通过，`21 / 21` 项通过
3. `npm run typecheck --workspace @novelstory/base-lab`
   - 结果：通过，退出码为 `0`
4. `npm run build --workspace @novelstory/base-lab`
   - 结果：通过，成功生成 `dist/` 产物

## 当前状态

1. `WBS 4.1.2 / 4.1.3 / 4.1.4` 仍处于 `待人工验收`。
2. 旧验收项保留为已通过，新补充的人工验收项保持未勾选，等待用户复核。
3. `WBS 4.1.5` 仍未启动。

## 后续建议

1. 用户下一轮人工验收时，优先复核新增四条补充人工验收项。
2. 若补充人工验收项通过，再考虑将卷宗独立验证结果带入 `WBS 4.1.5` 主工作台融合。
