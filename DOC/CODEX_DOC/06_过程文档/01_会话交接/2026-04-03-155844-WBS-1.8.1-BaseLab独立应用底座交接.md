# WBS 1.8.1 BaseLab独立应用底座交接

## 本轮目标

- 建立 `BaseLab` 独立应用壳、实验注册入口和共享视口工具

## 本轮完成

1. 新增 `apps/base-lab/` 独立 workspace
2. 新增 `packages/view-core/` 共享视口工具包
3. 根脚本已补入 `dev:base-lab`
4. `BaseLab` 首页已可展示实验卡片，并可打开 `WBS 3.1` 实验入口

## 当前状态

1. `WBS 1.8.1` 已自测通过，待人工验收
2. `WBS 1.8.2` 已可继续承接迁移工作

## 下一步建议

1. 人工验证 `BaseLab` 首页和入口组织是否符合预期
2. 若通过，则继续以 `BaseLab` 承接后续 `WBS 5.1`、`WBS 6.1` 实验入口
