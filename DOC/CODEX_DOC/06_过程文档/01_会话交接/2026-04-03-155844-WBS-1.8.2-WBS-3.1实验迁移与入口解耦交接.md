# WBS 1.8.2 WBS 3.1实验迁移与入口解耦交接

## 本轮目标

- 将现有 `WBS 3.1` 基座对比实验迁入 `BaseLab`，并从主工作台移除旧入口

## 本轮完成

1. `BaseSelectionLab` 已迁入 `apps/base-lab/src/components/`
2. `apps/web/src/App.tsx` 已移除旧 `WBS 3.1 Base Lab` 入口
3. `WBS 3.1` 设计文档与研制计划已更新为 `BaseLab` 独立入口口径
4. `BaseLab` 与 `web` 两侧关键测试和构建均已通过

## 当前状态

1. `WBS 1.8.2` 已自测通过，待人工验收
2. `WBS 3.1` 当前验证入口已切换为 `BaseLab` 独立应用

## 下一步建议

1. 人工核验主工作台入口已清理、`BaseLab` 实验入口可正常使用
2. 若通过，可继续把后续新的实验节点接入 `BaseLab`
