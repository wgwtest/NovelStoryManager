# 验收通过后状态同步策略自测

## 自测目标

验证本轮已经把“用户验收通过后，必须同轮同步 GitHub Project、issue 契约和本地状态文档”这条规则写入全局策略与项目策略，并已将 `WBS 1` 到 `WBS 2.1` 的实际状态同步到远端。

## 执行记录

1. 全局策略检查
   - 命令：`sed -n '18,90p' /home/wgw/.codex/skills/project-engineering-strategy/SKILL.md`
   - 结果：已存在 `Acceptance sync rule`，明确要求用户验收通过后同轮同步 tracker、execution contract 和 local docs
2. 项目策略检查
   - 命令：`sed -n '18,120p' DOC/CODEX_DOC/00-本地工程策略映射.md`
   - 结果：已明确写入 GitHub Project `Status` 同步规则，并写明本项目承认 `【】` 中的明确通过结论属于正式验收信号
3. 项目索引检查
   - 命令：`sed -n '1,40p' DOC/CODEX_DOC/README.md`
   - 结果：README 已补充“用户明确通过后必须同轮同步 Project 状态”的规则，当前主线已切换到 `WBS 0 -> WBS 2 -> WBS 2.1`
4. GitHub Project 状态检查
   - 命令：`env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh project item-list 3 --owner wgwtest --format json | jq '.items[] | select(.content.number==1 or .content.number==2 or .content.number==9 or .content.number==10 or .content.number==15 or .content.number==16 or .content.number==17 or .content.number==18 or .content.number==19) | {number:.content.number,title:.title,status:.status,workType:.\"work Type\",start:.\"start Date\",target:.\"target Date\"}'`
   - 结果：`#9`、`#15`、`#16`、`#17`、`#18`、`#19` 已为 `Done`；`#10`、`#2` 已为 `In Progress`
5. GitHub issue 状态检查
   - 命令：`for n in 1 2 8 9 10 15 16 17 18 19; do env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh issue view \"$n\" --repo wgwtest/NovelStoryManager --json number,title,state | jq '{number,title,state}'; done`
   - 结果：`#1`、`#9`、`#15`、`#16`、`#17`、`#18`、`#19` 为 `CLOSED`；`#8`、`#10`、`#2` 为 `OPEN`
6. GitHub issue 正文抽查
   - 命令：`for n in 15 2 9; do env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh issue view \"$n\" --repo wgwtest/NovelStoryManager --json body | jq -r '.body' | rg -n '当前状态：|已验收|待人工验收|开发中'; done`
   - 结果：`#15` 与 `#9` 正文为 `已验收`，`#2` 正文为 `待人工验收`

## 结论

1. 全局共享策略与本项目策略都已固化“验收通过后同轮同步状态”的规则
2. `WBS 1` 子树的 GitHub Project 和 issue 状态已与用户验收结果对齐
3. 当前主线已同步切换到 `WBS 2 / WBS 2.1`
