# WBS 1 原型先行重排自测

## 自测目标

验证 `WBS 1` 已经从“边界 + 技术方案 + 初始任务拆解”的三段式结构，重排为“边界 -> 信息架构 -> 低保真原型 -> 高风险交互验证 -> 技术方案选型 -> 初始任务拆解”的六节点链路，并且本地文档、GitHub issue 树、Project 时间规划保持一致。

## 执行记录

1. 本地变更范围检查
   - 命令：`git diff --name-status`
   - 结果：本轮差异集中在 `DOC/CODEX_DOC/` 下的 WBS 节点说明、设计稿、契约草案、README 和执行证据文档，没有越界到业务代码目录
2. 顶层 WBS 结构检查
   - 命令：`sed -n '1,220p' DOC/CODEX_DOC/01-WBS-L0-NovelStoryManager-研发总纲节点说明.md`
   - 结果：`WBS 1` 已明确拆成 `1.1` 到 `1.6` 六个子节点，并把技术方案选型放在原型与交互验证之后
3. 低保真原型检查
   - 命令：`sed -n '1,260p' DOC/CODEX_DOC/设计稿/2026-04-01-WBS-1.3-低保真原型设计.md`
   - 结果：正文已包含全局工作台、Knowledge、Graph、Tracks 四张 ASCII 草图，能够直接看出左侧对象库常驻、中心标签页和右侧检查器结构
4. 高风险交互验证检查
   - 命令：`sed -n '1,260p' DOC/CODEX_DOC/设计稿/2026-04-01-WBS-1.4-高风险交互原型验证.md`
   - 结果：已明确对象库拖拽、Graph 交互、Tracks 交互和通用编辑器交互四类验证项，并明确首版不优先采用通用 timeline 插件
5. 技术方案选型检查
   - 命令：`sed -n '1,260p' DOC/CODEX_DOC/设计稿/2026-04-01-WBS-1.5-技术方案选型.md`
   - 结果：已明确以上游 `WBS 1.2 / 1.3 / 1.4` 为输入，并冻结 `React + Vite + TypeScript`、`Fastify + Node.js`、`Zod`、`dnd-kit`、`React Flow`、`elkjs`、`react-resizable-panels`
6. GitHub issue 树与正文检查
   - 命令：`env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh api graphql -f query='query { repository(owner:"wgwtest", name:"NovelStoryManager") { issue(number:9){ title subIssues(first:20){ nodes { number title } } } issue15: issue(number:15){ title body } issue16: issue(number:16){ title body } issue17: issue(number:17){ title body } issue18: issue(number:18){ title body } issue19: issue(number:19){ title body } issue2: issue(number:2){ title body } } }'`
   - 结果：`#9` 的直接子 issue 为 `#1 #16 #15 #17 #18 #19`；`#15` 到 `#19` 与 `#2` 的正文已切换到新的 WBS 结构和契约模板
7. GitHub Project 时间规划检查
   - 命令：`env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh project item-list 3 --owner wgwtest --format json`
   - 结果：`#15` 为 `In Progress / Prep / 2026-04-01 -> 2026-04-03`；`#16` 为 `Todo / Prep / 2026-04-03 -> 2026-04-05`；`#17` 为 `Todo / Prep / 2026-04-05 -> 2026-04-07`；`#18` 为 `Todo / Prep / 2026-04-07 -> 2026-04-08`；`#19` 为 `Todo / Prep / 2026-04-08 -> 2026-04-09`；`#2` 为 `Todo / Core / 2026-04-09 -> 2026-04-16`

## 结论

1. `WBS 1` 已按“先原型、后选型”重新编排完成
2. 低保真原型和高风险交互验证文档已经落地，技术选型不再先于原型
3. GitHub issue 树和 Project 时间规划已与本地计划同步
