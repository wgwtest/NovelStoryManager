# WBS 2.1 项目底座基础版自测

## 自测目标

验证 `WBS 2.1 项目底座实施` 已交付首个可运行基础版，包括文件化单项目存储、schema 包、本地服务、常驻对象库、Knowledge 主视图，以及可工作的对象编辑保存链路。

## 执行记录

1. 工作区测试检查
   - 命令：`npm run test`
   - 结果：根工作区、`@novelstory/service`、`@novelstory/web`、`@novelstory/schema` 全部通过，退出码为 `0`
2. 类型检查
   - 命令：`npm run typecheck`
   - 结果：所有 workspace 类型检查通过，退出码为 `0`
3. 构建检查
   - 命令：`npm run build`
   - 结果：`@novelstory/service` 构建成功，`@novelstory/web` 完成 Vite 生产构建，退出码为 `0`
4. 本地服务烟测
   - 命令：`npm run dev:service`
   - 结果：服务监听 `http://127.0.0.1:3001`
5. 前端启动烟测
   - 命令：`npm run dev:web`
   - 结果：前端监听 `http://127.0.0.1:4173/`
6. 健康接口检查
   - 命令：`curl -s http://127.0.0.1:3001/api/health`
   - 结果：返回 `{"status":"ok"}`
7. 浏览器加载检查
   - 方法：打开 `http://127.0.0.1:4173/`
   - 结果：成功加载样例项目 `青云劫火录`，左侧对象库、Knowledge 表格、右侧 Inspector 均正常显示
8. 浏览器保存链路检查
   - 方法：在 Inspector 中修改 `char_suxuan.summary`，点击 `Save Object`，随后核对 `fixtures/projects/sample-novel/objects/characters.json`
   - 结果：文件内容被实时更新；完成验证后已恢复样例夹具原始摘要

## 结论

1. `WBS 2.1` 基础版已经具备本地运行能力
2. 对象库作为事实来源已通过文件存储链路打通
3. Graph / Tracks 当前保持占位入口，符合本节点范围约束
