# WBS 2.2 规划与状态同步自测

## 自测目标

验证本轮已经同时完成三件事：

1. 将 `WBS 2.1` 的验收通过同步到本地契约、GitHub issue 和 GitHub Project
2. 将 `WBS 2.2` 从抽象命名收紧为“数据规范与导入导出规范”
3. 将“命名直白、成果物落路径、数据规范任务必须有正反样例与往返测试”的规则写入项目策略和全局策略

## 执行记录

1. 本地状态核对
   - 命令：`sed -n '1,120p' DOC/CODEX_DOC/README.md`
   - 结果：当前活动链路已为 `WBS 0 -> WBS 2 -> WBS 2.2`，当前阶段状态为 `开发中`
2. 项目策略核对
   - 命令：`sed -n '150,210p' DOC/CODEX_DOC/00-本地工程策略映射.md`
   - 结果：已新增“叶子节点命名直白”“成果物必须落路径”“数据规范类任务必须包含正反样例和往返测试”等规则
3. 全局策略核对
   - 命令：`rg -n -e 'Prefer direct, human-scannable leaf names' -e 'valid sample case' -e '数据规范、文件规范、导入导出类任务的成果物' /home/wgw/.codex/skills/project-engineering-strategy/SKILL.md /home/wgw/.codex/skills/project-engineering-strategy/references/ENGINEERING_WORK_STRATEGY.md`
   - 结果：全局策略已补充“直白命名”“正样例/反样例/往返一致性测试”“成果物明确到规范正文/实现文件/样例路径”等规则
4. `WBS 2.1` 验收状态核对
   - 命令：`gh issue view 2 --json number,title,state,closed,projectItems,body`
   - 结果：issue `#2` 当前为 `CLOSED`，正文当前状态为 `已验收`，Project `Status` 为 `Done`
5. `WBS 2.2` 远端状态核对
   - 命令：`gh issue view 3 --json number,title,state,closed,projectItems,body`
   - 结果：issue `#3` 当前为 `OPEN`，标题已更新为 `WBS 2.2 数据规范与导入导出规范`，Project `Status` 为 `In Progress`
6. `WBS 2` 父节点核对
   - 命令：`gh issue view 10 --json number,title,state,projectItems,body`
   - 结果：issue `#10` 已同步新子节点名称和 `WBS 2.2` 计划文档路径，Project `Status` 保持 `In Progress`
7. 新规划文档核对
   - 命令：`sed -n '1,220p' DOC/CODEX_DOC/开发计划/04-WBS-L2-WBS-2.2-数据规范与导入导出规范计划.md`
   - 结果：计划文档已明确成果物路径、实施分块和强制测试矩阵

## 结论

1. `WBS 2.1` 的验收通过状态已完成三层同步
2. `WBS 2.2` 的命名、成果物和测试要求已从抽象描述收紧为可执行契约
3. 当前可以直接进入 `WBS 2.2` 的实现阶段，或先由用户对规划文档进行人工评阅
