# WBS 树与时间规划重排自测

## 自测目标

验证本轮“去里程碑、补技术选型与时间规划、重排 GitHub Project 树”是否已经在本地文档和 GitHub 跟踪层同时生效。

## 执行记录

1. 本地活动文档残留检查
   - 命令：`rg -n "03-WBS-L2-WBS-2.1-M1|02-WBS-L1-WBS-1.1-项目启动与M1前置规划|WBS 2.1 M1|WBS 1 项目启动与工程基线|项目启动与 M1 前置规划" DOC/CODEX_DOC -g '!会话交接/**' -g '!验收清单/**' -g '!自测报告/**'`
   - 结果：无匹配，说明活动文档已去掉旧命名
2. GitHub issue 树检查
   - 命令：`gh api graphql -f query='query { repository(owner:"wgwtest", name:"NovelStoryManager") { issues(first:40, orderBy:{field:CREATED_AT, direction:ASC}) { nodes { number title state parent { number title } subIssues(first:10) { nodes { number title } } } } } }'`
   - 结果：确认 `#9 WBS 1 需求与方案` 下已挂 `#1 #15 #16`
3. GitHub Project 当前主线检查
   - 命令：`gh project item-list 3 --owner wgwtest --format json --jq '.items[] | select(.content.number==2 or .content.number==9 or .content.number==10 or .content.number==15 or .content.number==16) | {number:.content.number,title:.title,status:.status,phase:.phase,start:."start Date",target:."target Date",workType:."work Type"}'`
   - 结果：确认当前主线节点 `#15` 为 `In Progress`，`phase` 已清空，`#9/#15/#16/#10/#2` 均具备正确状态或日期

## 结论

1. 本地文档已切换为 `WBS + Depends On + Start/Target Date` 模式
2. GitHub issue 树已补出 `WBS 1.2` 与 `WBS 1.3`
3. Project 已去掉旧里程碑值显示，并把当前主线切回 `WBS 1`
