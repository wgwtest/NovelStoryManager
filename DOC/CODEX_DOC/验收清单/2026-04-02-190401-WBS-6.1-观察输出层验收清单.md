# WBS 6.1 观察输出层验收清单

## 验收范围

1. `DOC/CODEX_DOC/契约草案/2026-03-30-WBS-6.1-观察输出层.md`
2. `DOC/CODEX_DOC/开发计划/08-WBS-L2-WBS-6.1-观察输出层计划.md`
3. `apps/web/src/lib/project-observation.ts`
4. `apps/web/src/components/TracksView.tsx`
5. `apps/web/src/App.test.tsx`
6. `apps/web/src/styles.css`

## 人工验收项

- [ ] Tracks 工作台中存在 `Observation Mode` 选择器和 `Observation Workspace` 面板
- [ ] 选择 `time` 时，可看到时间切片 `卷一-早期`
- [ ] 选择 `character` 时，可看到人物切片 `苏玄`
- [ ] 选择 `location` 时，可看到地点切片 `青云峰`
- [ ] 选择 `arc` 时，可看到剧情线切片 `隐火灵脉主线`
- [ ] 面板中存在 `Chapter Dimension` 预留位，并提示未来结构化章节切片接入
- [ ] 观察结果只用于展示和跳转，不会直接改写对象事实文件
- [ ] 运行 `npm run test --workspace @novelstory/web`，结果退出码为 `0`
- [ ] 运行 `npm run typecheck`，结果退出码为 `0`
- [ ] 运行 `npm run test`，结果退出码为 `0`
- [ ] 运行 `npm run build`，结果退出码为 `0`
- [ ] GitHub issue `#7` 当前状态为 `OPEN`，正文当前状态为 `待人工验收`
- [ ] GitHub Project 中 `#7` 当前 `Status` 为 `In Progress`
