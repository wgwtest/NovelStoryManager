# WBS 5 多轨编排视图

工作目标：

- 维护多轨编排视图相关实现子树，让事件编排、轨道分组和预设管理在同一模块下推进。

工作内容：

- 维护 `#6 WBS 5.1 多轨编排视图` 及其后续子节点。
- 明确轨道视图中的编排状态与事件事实分离。
- 明确轨道分组策略属于观察和编排逻辑，不直接改写对象真源。

潜在难点：

- 拖拽交互最容易直接修改底层事实，造成模型污染。
- 多轨分组一旦边界不清，后续会和观察输出层职责冲突。

Depends On：

- `WBS 2` 核心对象模型与文件化项目

成果物：

- `#6` 子 issue
- Tracks 视图相关模块与预设存储约束

验收方法：

- 人工核验 `#13` 的直接子 issue 至少包含 `#6`。
- 人工核验 `#6` issue 正文已明确写出代码验收方法。

当前状态：

- 待开发

上级节点：

- WBS 0
