# WBS 2 核心对象模型与文件化项目

工作目标：

- 建立对象模型、文件化项目结构和项目底座实现子树，为后续三个主视图提供统一的数据真源和本地存储链路。

工作内容：

- 维护 `#2 WBS 2.1 项目底座实施` 和 `#3 WBS 2.2 Schema 与导入导出契约增强` 两个子节点。
- 明确 schema、项目读写、导入导出三类工作边界。
- 保证对象事实和视图状态始终分离存储。

潜在难点：

- 对象模型、项目读写和导入导出之间高度耦合，边界不清时会引起返工。
- 如果项目底座阶段把视图状态写进对象事实，后续关系图和轨道视图会被污染。

Depends On：

- `WBS 1` 需求与方案

成果物：

- `#2`、`#3` 子 issue
- `DOC/CODEX_DOC/开发计划/03-WBS-L2-WBS-2.1-项目底座实施计划.md`
- `packages/schema/`、`apps/service/`、`fixtures/projects/` 相关实现

验收方法：

- 人工核验 `#10` 的直接子 issue 为 `#2 #3`。
- 人工核验 [03-WBS-L2-WBS-2.1-项目底座实施计划.md](/home/wgw/CodexProject/NovelStoryManager/DOC/CODEX_DOC/开发计划/03-WBS-L2-WBS-2.1-项目底座实施计划.md) 已存在。
- 人工核验 `#2` 和 `#3` 的 issue 正文已改为新模板，并分别写出代码验收方法。

当前状态：

- 待开发

上级节点：

- WBS 0
