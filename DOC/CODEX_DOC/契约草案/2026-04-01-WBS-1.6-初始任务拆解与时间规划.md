# WBS 1.6 初始任务拆解与时间规划

工作目标：

- 把首轮执行链路拆到可执行 issue 层级，并为新的主线补齐依赖关系、状态和时间规划。

工作内容：

- 将当前主线整理为 `#9 -> #15 -> #16 -> #17 -> #18 -> #19 -> #10 -> #2`。
- 为 `#9`、`#15`、`#16`、`#17`、`#18`、`#19`、`#10`、`#2` 写入状态、开始时间和目标时间。
- 清理 Project 中不再使用的里程碑字段，保持只用 `WBS + Depends On + 日期` 推进。

潜在难点：

- 既要表达先后顺序，又不能重新引入第二套里程碑导航轴。
- 文档、issue 和 Project 三处信息容易不同步。

Depends On：

- `WBS 1.5` 技术方案选型

成果物：

- `DOC/CODEX_DOC/07-WBS-L1-WBS-1.6-初始任务拆解与时间规划节点说明.md`
- GitHub Project 中 `#9`、`#15`、`#16`、`#17`、`#18`、`#19`、`#10`、`#2` 的状态和日期
- `#19` issue 正文

验收方法：

- 人工核验 [07-WBS-L1-WBS-1.6-初始任务拆解与时间规划节点说明.md](/home/wgw/CodexProject/NovelStoryManager/DOC/CODEX_DOC/07-WBS-L1-WBS-1.6-初始任务拆解与时间规划节点说明.md) 中存在“当前主线时间规划”和“当前主线依赖”两节。
- 人工核验 GitHub Project 中：
  - `#15` 为 `In Progress`
  - `#16` 为 `Todo`
  - `#17` 为 `Todo`
  - `#18` 为 `Todo`
  - `#19` 为 `Todo`
  - `#2` 为 `Todo`
  - 上述 issue 已填写 `Work Type`、`Start Date` 和 `Target Date`

当前状态：

- 待开发

上级节点：

- WBS 1. 需求与方案
