# WBS 4 关系图工作台

工作目标：

- 维护关系图视图相关实现子树，让对象关系可视化、边编辑和布局持久化在同一模块下推进。

工作内容：

- 维护 `#5 WBS 4.1 关系图工作台` 及其后续子节点。
- 明确图布局属于视图状态，不直接写回对象事实。
- 明确关系图与知识库、轨道视图的协作边界。

潜在难点：

- 图布局、图过滤和对象关系本体容易混在一起。
- 关系边编辑如果没有清楚的模型映射，会导致图上操作与对象真源脱节。

Depends On：

- `WBS 2` 核心对象模型与文件化项目

成果物：

- `#5` 子 issue
- Graph 视图相关模块与布局存储约束

验收方法：

- 人工核验 `#12` 的直接子 issue 至少包含 `#5`。
- 人工核验 `#5` issue 正文已明确写出代码验收方法。

当前状态：

- 待开发

上级节点：

- WBS 0
