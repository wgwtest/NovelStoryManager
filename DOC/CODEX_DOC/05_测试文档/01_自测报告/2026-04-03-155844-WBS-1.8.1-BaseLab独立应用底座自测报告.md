# WBS 1.8.1 BaseLab独立应用底座自测报告

## 自测范围

1. `package.json`
2. `scripts/workspace-smoke.test.mjs`
3. `packages/view-core/src/index.ts`
4. `packages/view-core/src/index.test.ts`
5. `apps/base-lab/`

## 自测命令

1. `node --test scripts/workspace-smoke.test.mjs`
   - 结果：通过
   - 说明：根脚本已包含 `dev:base-lab`
2. `npm run test --workspace @novelstory/view-core`
   - 结果：通过
   - 说明：`view-core` 的视口创建、缩放、平移、尺寸归一化测试全部通过
3. `npm run test --workspace @novelstory/base-lab`
   - 结果：通过
   - 说明：`BaseLab` 首页卡片展示与实验打开/返回流程通过
4. `npm run typecheck --workspace @novelstory/base-lab`
   - 结果：通过
5. `npm run build --workspace @novelstory/base-lab`
   - 结果：通过
   - 说明：独立应用成功产出构建产物

## 结论

1. `BaseLab` 独立应用底座已建立完成。
2. `view-core` 已从 `apps/web` 内部工具抽出，可被主工作台与 `BaseLab` 共同复用。
3. `WBS 1.8.1` 当前可进入人工验收。
