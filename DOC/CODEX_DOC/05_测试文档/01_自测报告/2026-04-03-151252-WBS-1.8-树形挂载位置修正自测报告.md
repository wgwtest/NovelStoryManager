# WBS 1.8 树形挂载位置修正自测报告

## 自测范围

1. GitHub issue `#39 WBS 1.8 BaseLab验证工具构建方案`
2. GitHub issue `#9 WBS 1 需求与方案`
3. GitHub Project `NovelStoryManager Delivery Roadmap`
4. `DOC/CODEX_DOC/06_过程文档/03_验收意见处理/2026-04-03-151252-WBS-1.8-树形挂载位置修正.md`

## 执行命令

1. `gh api graphql -f query='query { repository(owner:"wgwtest", name:"NovelStoryManager") { issue(number: 39) { number title parent { number title } } issue9: issue(number: 9) { number title subIssues(first: 20) { nodes { number title } } } } }'`
2. `gh api graphql -f query='query { user(login: "wgwtest") { projectV2(number: 3) { items(first: 100) { nodes { id content { ... on Issue { number title url } } fieldValues(first: 10) { nodes { ... on ProjectV2ItemFieldSingleSelectValue { field { ... on ProjectV2SingleSelectField { name } } name } ... on ProjectV2ItemFieldDateValue { field { ... on ProjectV2Field { name } } date } } } } } } } }' --jq '.data.user.projectV2.items.nodes[] | select(.content.number == 39 or .content.number == 9)'`

## 自测结果

1. `#39.parent` 已为 `#9 WBS 1 需求与方案`
2. `#9.subIssues` 已包含 `#39 WBS 1.8 BaseLab验证工具构建方案`
3. GitHub Project 中 `#39` 条目仍存在，字段未丢失：
   - `Status = In Progress`
   - `Work Type = Prep`
   - `Start Date = 2026-04-03`
   - `Target Date = 2026-04-04`

## 结论

1. 本次问题的根因是“缺少原生子 issue 关系”，不是 Project 条目缺失。
2. 当前树形挂载位置已修正完成。
