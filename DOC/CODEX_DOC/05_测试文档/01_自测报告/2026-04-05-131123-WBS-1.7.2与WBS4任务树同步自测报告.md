# WBS 1.7.2 与 WBS 4 任务树同步自测报告

## 自测范围

1. `DOC/CODEX_DOC/README.md`
2. `DOC/CODEX_DOC/00-本地工程策略映射.md`
3. `DOC/CODEX_DOC/04_研发文档/03_原型图/WBS-1.7.2/WBS-1.7.2-原型稿执行说明.md`
4. `DOC/CODEX_DOC/05_测试文档/01_自测报告/2026-04-05-123524-WBS-1.7.2-四张核心工作面自测报告.md`
5. `DOC/CODEX_DOC/06_过程文档/03_验收意见处理/2026-04-05-122035-WBS-1.7.2-四张核心图与视图任务树重排分析.md`
6. GitHub issue `#8`
7. GitHub issue `#43`
8. GitHub issue `#12`
9. GitHub issue `#44`
10. GitHub issue `#13`
11. GitHub issue `#14`
12. GitHub Project `https://github.com/users/wgwtest/projects/3`

## 执行记录

### 1. 原型脚本验证

```bash
node --test scripts/generate-wbs-1.7.2-prototypes.test.mjs scripts/export-wbs-1.7.2-prototypes.test.mjs
node scripts/generate-wbs-1.7.2-prototypes.mjs
node scripts/export-wbs-1.7.2-prototypes.mjs
```

预期结果：

1. 命令退出码均为 `0`
2. 当前有效原型目录只保留 `4` 个工作面
3. 当前有效 PNG 总数为 `4`
4. 重新生成原型后，根目录下的 `WBS-1.7.2-原型稿执行说明.md` 仍然保留

### 2. 远端 issue 正文验证

```bash
gh issue view 43 --repo wgwtest/NovelStoryManager --json number,title,body,comments
gh issue view 8 --repo wgwtest/NovelStoryManager --json number,title,body
gh issue view 11 --repo wgwtest/NovelStoryManager --json number,title,body
```

预期结果：

1. `#43` 当前状态为 `待人工验收`
2. `#43` 最新评论已回写 `4188222166`
3. `#8` 当前主线已改为 `WBS 0 -> WBS 1 -> WBS 1.7 -> WBS 1.7.2`
4. `#11` 标题与正文统一使用 `多视图共性机制`

### 3. WBS 树验证

```bash
gh api graphql -f query='query { repository(owner:"wgwtest", name:"NovelStoryManager") { i12: issue(number: 12) { number title subIssues(first: 20) { nodes { number title } } } i44: issue(number: 44) { number title subIssues(first: 20) { nodes { number title } } } i13: issue(number: 13) { number title subIssues(first: 20) { nodes { number title } } } i14: issue(number: 14) { number title subIssues(first: 20) { nodes { number title } } } } }'
```

预期结果：

1. `#12` 子节点顺序为 `#44 -> #13 -> #14`
2. `#44` 子节点顺序为 `#5 -> #25 -> #26 -> #27`
3. `#14` 子节点顺序为 `#7 -> #31 -> #32 -> #33`

### 4. Project 条目验证

```bash
gh api graphql -f query='query($id:ID!){ node(id:$id) { __typename ... on ProjectV2Item { id isArchived type content { __typename ... on Issue { number title url } } fieldValues(first:20) { nodes { __typename ... on ProjectV2ItemFieldSingleSelectValue { field { ... on ProjectV2SingleSelectField { name } } name } ... on ProjectV2ItemFieldDateValue { field { ... on ProjectV2FieldCommon { name } } date } } } } } }' -F id=PVTI_lAHOD9ycyM4BTOqkzgpInEY
gh api graphql -f query='query($id:ID!){ node(id:$id) { __typename ... on ProjectV2Item { id isArchived type content { __typename ... on Issue { number title url } } fieldValues(first:20) { nodes { __typename ... on ProjectV2ItemFieldSingleSelectValue { field { ... on ProjectV2SingleSelectField { name } } name } ... on ProjectV2ItemFieldDateValue { field { ... on ProjectV2FieldCommon { name } } date } } } } } }' -F id=PVTI_lAHOD9ycyM4BTOqkzgpBXAg
gh api graphql -f query='query($id:ID!){ node(id:$id) { __typename ... on ProjectV2Item { id isArchived type content { __typename ... on Issue { number title url } } fieldValues(first:20) { nodes { __typename ... on ProjectV2ItemFieldSingleSelectValue { field { ... on ProjectV2SingleSelectField { name } } name } ... on ProjectV2ItemFieldDateValue { field { ... on ProjectV2FieldCommon { name } } date } } } } } }' -F id=PVTI_lAHOD9ycyM4BTOqkzgpBXUU
```

预期结果：

1. `PVTI_lAHOD9ycyM4BTOqkzgpInEY` 对应 issue `#44`
2. `PVTI_lAHOD9ycyM4BTOqkzgpBXAg` 对应 issue `#32`
3. `PVTI_lAHOD9ycyM4BTOqkzgpBXUU` 对应 issue `#33`
4. 三个条目的 `Status` 均为 `In Progress`
5. 三个条目的 `Work Type` 均为 `Core`
6. 三个条目的 `Start Date` 均为 `2026-04-03`

## 结论

1. `WBS 1.7.2` 的四张核心工作面口径已经和本地文档、原型目录、脚本输出、GitHub issue 保持一致。
2. `WBS 4` 视图设计树已经按 `4.1 / 4.2 / 4.3` 重排完成，父子顺序正确。
3. GitHub Project 中新增的 `#44 / #32 / #33` 已有独立条目与字段值。
4. `generate-wbs-1.7.2-prototypes.mjs` 已修正为只清理根目录下的生成目录，不再误删根目录说明文档。
5. 本轮结果已达到人工验收前提，相关节点应保持 `待人工验收` 或 `In Progress`，不提前标记为完成。
