# WBS 3-7 三视图共性至观察输出层自测

## 自测范围

1. `apps/web/src/App.tsx`
2. `apps/web/src/App.test.tsx`
3. `apps/web/src/components/GraphView.tsx`
4. `apps/web/src/components/TracksView.tsx`
5. `apps/web/src/components/ObjectLibrary.tsx`
6. `apps/web/src/lib/project-observation.ts`
7. `apps/web/src/lib/project-observation.test.ts`
8. `apps/web/src/lib/project-tracks.ts`
9. `apps/web/src/lib/view-canvas.ts`
10. `apps/service/src/app.ts`

## 重点结论

1. 知识库、关系图、多轨视图和观察输出层已形成完整前端工作流。
2. 章节继续作为观察层切片，不反向写入对象真源。
3. 多轨事件换轨已落到事件对象引用字段更新，章节切片保存已落到 `views/chapter-slices.json`。

## 命令与结果

1. `npm test --workspace @novelstory/schema`
   - 结果：通过，`2` 个测试文件、`20` 个测试用例全部通过
2. `npm test --workspace @novelstory/service`
   - 结果：通过，`1` 个测试文件、`12` 个测试用例全部通过
3. `npm test --workspace @novelstory/web`
   - 结果：通过，`3` 个测试文件、`18` 个测试用例全部通过
4. `npm run typecheck`
   - 结果：通过，所有 workspace 类型检查通过
5. `npm run build`
   - 结果：通过，`apps/web` 产物已完成构建

## 本轮新增覆盖点

1. 验证对象库可直接创建对象。
2. 验证多轨视图的 `Create Event` 会创建真正的事件对象，而不是误创建其他对象。
3. 验证事件块拖拽换轨会触发事件对象补丁保存。
4. 验证观察输出支持 `time / character / location / arc / chapter` 五种模式。
5. 验证章节切片可显示相关人物、地点、剧情弧，并可保存章节正文。

## 风险说明

1. 当前尚未执行用户人工验收，GitHub Project / issue 状态未更新为完成。
2. 人工测试时仍应重点观察拖拽手感、画布平移缩放体验和章节编辑保存后的可视反馈。
