# WBS 4 远端评阅意见同步自测

## 范围

本轮主要同步以下内容：

1. 本地设计说明、研制计划、索引文档
2. GitHub issue 正文与处理评论
3. GitHub Project 子树与状态投影

本轮未修改业务代码，因此未执行前端构建、单元测试或运行时页面验证。

## 执行记录

### 1. 本地文档差异检查

- 命令：`git diff --check`
- 结果：通过，无空白错误、无冲突标记

### 2. 卷宗子树验证

- 命令：`gh api graphql -f query='query { repository(owner:"wgwtest", name:"NovelStoryManager") { issue(number:44) { subIssues(first:20) { nodes { number title } } } } }'`
- 结果：通过，`#44` 当前子节点为 `#5 / #25 / #26 / #27 / #50`

### 3. Project 状态验证

- 命令：`gh project item-list 3 --owner wgwtest --limit 200 --format json | jq '.items[] | select(.content.number==12 or .content.number==25 or .content.number==26 or .content.number==27 or .content.number==44 or .content.number==50) | {number:.content.number,title:.content.title,status:.status,workType:."work Type",start:."start Date"}'`
- 结果：通过
- 关键结论：
  - `#12` 为 `In Progress`
  - `#44` 为 `In Progress`
  - `#25 / #26 / #27 / #50` 为 `Todo`
  - `#50` 已进入 Project，`Work Type` 为 `Core`

### 4. Issue 正文验证

- 命令：`gh issue view 3 --repo wgwtest/NovelStoryManager --json title,body`
- 结果：通过，已补入“增量扩展原则”与下游 `WBS 3 / 4 / 7` 口径

- 命令：`gh issue view 12 --repo wgwtest/NovelStoryManager --json title,body`
- 结果：通过，标题已更新为 `WBS 4 多视图设计`，正文已写入“当前首批四个工作面”和“模型增量补齐策略”

- 命令：`gh issue view 44 --repo wgwtest/NovelStoryManager --json title,body`
- 结果：通过，正文已写入 `BaseLab + 模拟数据` 路线和 `WBS 4.1.5`

### 5. 索引文档状态验证

- 命令：`sed -n '1,40p' DOC/CODEX_DOC/README.md`
- 结果：通过，当前活动链路已更新为 `WBS 0 -> WBS 4 -> WBS 4.1 -> WBS 4.1.2/4.1.3/4.1.4 -> WBS 4.1.5`

## 自测结论

1. 本地文档已同步到新的多视图与卷宗优先口径。
2. GitHub issue 正文、评论、子树和 Project 投影已闭环。
3. 当前可以在不重新梳理计划的前提下，直接进入 `WBS 4.1.2` 的 `BaseLab` 独立验证实现。
