# WBS 1.8.2 拖拽与动态连线修正自测报告

## 自测范围

1. `packages/view-core/src/index.ts`
2. `packages/view-core/src/index.test.ts`
3. `apps/base-lab/src/lib/base-selection-lab.ts`
4. `apps/base-lab/src/lib/base-selection-lab.test.ts`
5. `apps/base-lab/src/components/BaseSelectionLab.tsx`
6. `apps/base-lab/src/components/BaseSelectionLab.test.tsx`
7. `apps/base-lab/src/styles.css`
8. `DOC/CODEX_DOC/02_设计说明/2026-04-03-WBS-3.1-交互画布与渲染基座选型.md`
9. `DOC/CODEX_DOC/03_研制计划/08D-WBS-1.8.2-WBS-3.1实验迁移与入口解耦-研制计划.md`
10. `DOC/CODEX_DOC/03_研制计划/13-WBS-3.1-交互画布与渲染基座选型-研制计划.md`

## 自测命令

1. `npm run test --workspace @novelstory/view-core`
   - 结果：通过
   - 说明：覆盖视口平移按原始 offset 重算，不再累计漂移
2. `npm run typecheck --workspace @novelstory/view-core`
   - 结果：通过
3. `npm run build --workspace @novelstory/view-core`
   - 结果：通过
4. `npm run test --workspace @novelstory/base-lab`
   - 结果：通过
   - 说明：覆盖端口命中、动态连线建边、DOM 画布平移和 SVG 连线新增
5. `npm run typecheck --workspace @novelstory/base-lab`
   - 结果：通过
6. `npm run build --workspace @novelstory/base-lab`
   - 结果：通过
7. `npm run test --workspace @novelstory/web -- src/App.test.tsx`
   - 结果：通过
   - 说明：主工作台未被 `BaseLab` 交互修正回归影响
8. `npm run typecheck --workspace @novelstory/web`
   - 结果：通过
9. `npm run build --workspace @novelstory/web`
   - 结果：通过

## 结论

1. 共享视口平移算法已修正为“初始 offset + 指针位移”，当前测试未再出现拖拽漂移。
2. `BaseLab` 的 `DOM + CSS`、`DOM + SVG`、`Canvas` 三种基座已接入同一套端口动态拖线状态机。
3. `DOM + SVG` 层已具备新增连线的界面级回归测试。
4. 当前变更已达到再次人工验收条件。
