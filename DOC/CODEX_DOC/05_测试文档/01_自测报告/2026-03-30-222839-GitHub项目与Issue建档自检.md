# GitHub 项目与 Issue 建档自检

## 范围

本次自检覆盖：

1. GitHub Project v2 创建与公开可见
2. 7 个 WBS issue 创建
3. issue 已加入 Project 并写入字段
4. 本地 `契约草案/` 与远端 issue 一一对应

## 执行的检查

1. `gh issue list --repo wgwtest/NovelStoryManager --limit 20`
   - 结果：确认 `#1` 到 `#7` 已创建

2. `gh project item-list 3 --owner wgwtest --limit 20 --format json`
   - 结果：确认 7 个 issue 已加入 Project
   - 结果：确认字段已写入
     - `#1`：`Status=Done`、`Phase=Planning`、`Work Type=Prep`
     - `#2`：`Status=In Progress`、`Phase=M1`、`Work Type=Core`
     - `#3-#5`：`Status=Todo`、`Phase=M2`、`Work Type=Core`
     - `#6`：`Status=Todo`、`Phase=M3`、`Work Type=Core`
     - `#7`：`Status=Todo`、`Phase=M4`、`Work Type=Core`

3. 本地文档核对
   - 结果：`DOC/CODEX_DOC/03_研制计划/` 下 7 份文档已创建

## 自检结论

1. Tracker 层已建立
2. Execution Contract 层已建立
3. Local Docs 与 GitHub Tracker 已形成回链

## 未覆盖项

1. 未创建 milestone
2. 未创建 GitHub labels
