# WBS 1 子节点顺序校正自测

## 自测目标

确认 GitHub 中 `WBS 1` 的原生子 issue 顺序已按 WBS 编号排正，且这次调整只影响显示顺序，不影响任务状态与时间规划。

## 执行记录

1. 调整前顺序检查
   - 命令：`env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh api graphql -f query='query { repository(owner:"wgwtest", name:"NovelStoryManager") { issue(number:9){ subIssues(first:20){ nodes { number title id } } } } }'`
   - 结果：`#9` 下的顺序为 `#1 -> #16 -> #15 -> #17 -> #18 -> #19`
2. 状态与日期抽样检查
   - 命令：`env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh project item-list 3 --owner wgwtest --format json | jq '.items[] | select(.content.number==15 or .content.number==16) | {number:.content.number,title,status,start:."start Date",target:."target Date"}'`
   - 结果：`#15` 仍为 `In Progress / 2026-04-01 -> 2026-04-03`，`#16` 仍为 `Todo / 2026-04-03 -> 2026-04-05`
3. 顺序校正
   - 命令：`env http_proxy=http://127.0.0.1:10809 https_proxy=http://127.0.0.1:10809 gh api graphql -f query='mutation($input: ReprioritizeSubIssueInput!) { reprioritizeSubIssue(input: $input) { issue { number title subIssues(first:20){ nodes { number title } } } } }' -F input[issueId]='I_kwDOR0ravM74oZs1' -F input[subIssueId]='I_kwDOR0ravM74rSrb' -F input[beforeId]='I_kwDOR0ravM74rSrt'`
   - 结果：返回结果显示 `#9` 的顺序已变为 `#1 -> #15 -> #16 -> #17 -> #18 -> #19`

## 结论

1. `WBS 1.2` 与 `WBS 1.3` 的显示顺序已校正
2. 这次调整没有特殊业务含义，只是修正原生子 issue 的展示顺序
3. `#15` 与 `#16` 的状态和时间规划未受影响
