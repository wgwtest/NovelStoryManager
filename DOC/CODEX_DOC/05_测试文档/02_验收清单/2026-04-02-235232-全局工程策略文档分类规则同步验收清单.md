# 全局工程策略文档分类规则同步验收清单

## 验收对象

1. `/home/wgw/.codex/skills/project-engineering-strategy/SKILL.md`
2. `/home/wgw/.codex/skills/project-engineering-strategy/references/ENGINEERING_WORK_STRATEGY.md`
3. `/home/wgw/.codex/skills/project-engineering-strategy/references/PROJECT_STARTUP_TEMPLATE.md`
4. `DOC/CODEX_DOC/00-本地工程策略映射.md`

## 验收项

- [ ] 检查 `SKILL.md`，确认项目文档根默认优先 `DOC/CODEX_DOC/`，且已写入 `01_需求分析` 到 `06_过程文档` 六大类目录结构。
- [ ] 检查 `SKILL.md`，确认执行契约默认字段已改为 `工作目标 / 工作内容 / 潜在难点 / Depends On / 成果物 / 验收方法 / 当前状态 / 上级节点`。
- [ ] 检查 `ENGINEERING_WORK_STRATEGY.md`，确认其已声明自己是当前共享工程策略基线，并写入 `02_设计说明` 平铺、`03_研制计划` 一节点一文档、`05_测试文档` 与 `06_过程文档` 的编号目录规则。
- [ ] 检查 `PROJECT_STARTUP_TEMPLATE.md`，确认新项目启动模板已改为六大类目录骨架，而不是旧的 `开发计划 / 验收清单 / 会话交接` 扁平目录。
- [ ] 检查 `DOC/CODEX_DOC/00-本地工程策略映射.md`，确认已新增“全局继承关系”章节，明确本项目规则已上升为共享工程策略基线。
- [ ] 运行旧模板字段残留检查命令，确认无输出：
  `cd /home/wgw && rg -n '^- \`验收入口\`|^- \`验收标准\`|^- \`目标\`|^- \`输出物\`|^目标：|^验收入口：|^验收标准：' .codex/skills/project-engineering-strategy -g '*.md' || true`

## 验收结论

- 待人工验收
