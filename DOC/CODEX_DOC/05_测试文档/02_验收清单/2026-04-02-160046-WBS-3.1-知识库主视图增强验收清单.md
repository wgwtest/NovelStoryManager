# WBS 3.1 知识库主视图增强验收清单

## 验收范围

1. `DOC/CODEX_DOC/03_研制计划/13-WBS-3.1-知识库主视图增强-研制计划.md`
2. `DOC/CODEX_DOC/03_研制计划/13-WBS-3.1-知识库主视图增强-研制计划.md`
3. `apps/web/src/App.tsx`
4. `apps/web/src/components/ObjectLibrary.tsx`
5. `apps/web/src/components/KnowledgeView.tsx`
6. `apps/web/src/components/ObjectInspector.tsx`
7. `apps/web/src/App.test.tsx`
8. `apps/web/src/styles.css`

## 人工验收项
- [√] 左侧对象库存在 `Filter Objects` 输入框，输入关键字后，对象列表和知识表同步过滤
- [√] 当前对象被过滤掉时，检查器会自动切到当前过滤结果中的首个对象
- [×] 检查器存在 `Core Fields` 和 `References` 两个分组；存在剩余字段时，会出现 `Additional Fields`【你当前版本的排版问题巨大，我基本看不到References分组的内容，都挤到屏幕下方去了】
- [ ] 从 `苏玄` 的 `青云宗` 引用点击跳转后，左侧类型切到 `factions`，检查器展示 `青云宗`【这个描述太令人迷茫了，我点击苏玄看不到青云宗】
- [ ] 运行 `npm run test --workspace @novelstory/web`，结果退出码为 `0`
- [ ] 运行 `npm run typecheck`，结果退出码为 `0`
- [ ] 运行 `npm run test`，结果退出码为 `0`
- [ ] 运行 `npm run build`，结果退出码为 `0`【这些npm，我没有验收的必要】
- [ ] GitHub issue `#4` 当前状态为 `OPEN`，正文当前状态为 `待人工验收`
- [ ] GitHub Project 中 `#4` 当前 `Status` 仍为 `In Progress`
