# WBS 2.1 项目底座基础版验收清单

## 验收范围

1. `DOC/CODEX_DOC/03_研制计划/10-WBS-2.1-项目底座实施-研制计划.md`
2. `packages/schema/`
3. `apps/service/`
4. `apps/web/`
5. `fixtures/projects/sample-novel/`

## 人工验收项

- [ ] 运行 `npm run typecheck`，结果退出码为 `0`
- [ ] 运行 `npm run test`，结果退出码为 `0`
- [ ] 运行 `npm run build`，结果退出码为 `0`
- [ ] 同时启动 `npm run dev:service` 与 `npm run dev:web` 后，浏览器可打开 `http://127.0.0.1:4173/`
- [ ] 左侧 `Object Library` 在三个 tab 下均常驻显示
- [ ] `Knowledge` tab 可展示样例项目对象列表，并在右侧 Inspector 中编辑对象字段
- [ ] 点击 `Save Object` 后，对应 JSON 文件内容发生更新
- [ ] `Graph` 与 `Tracks` tab 当前仅显示后续节点占位说明
【验收通过】