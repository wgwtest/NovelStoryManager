# WBS 1.8.2 拖拽与动态连线修正验收清单

## 验收范围

1. `BaseLab` 独立应用中的 `WBS 3.1 基座对比验证`
2. `DOC/CODEX_DOC/03_研制计划/08D-WBS-1.8.2-WBS-3.1实验迁移与入口解耦-研制计划.md`
3. `DOC/CODEX_DOC/03_研制计划/13-WBS-3.1-交互画布与渲染基座选型-研制计划.md`
4. `DOC/CODEX_DOC/02_设计说明/2026-04-03-WBS-3.1-交互画布与渲染基座选型.md`

## 人工验收项

- [ ] 在 `http://127.0.0.1:4174/` 打开 `BaseLab` 后，仍可进入 `WBS 3.1 基座对比验证`
- [ ] `DOM + CSS`、`DOM + SVG`、`Canvas` 三块验证画布均可正常显示
- [ ] 在任一画布中拖动画布时，不会出现“轻拖即大幅飘移”的异常
- [ ] 在 `DOM + CSS` 画布中，从一个输出端口拖向目标输入端口时，能看到动态连线预览，并在释放后形成新连线
- [ ] 在 `DOM + SVG` 画布中，从一个输出端口拖向目标输入端口时，能看到动态连线预览，并在释放后形成新连线
- [ ] 在 `Canvas` 画布中，从一个输出端口拖向目标输入端口时，能看到动态连线预览，并在释放后形成新连线
- [ ] 点击 `Back to BaseLab` 后，能够返回 `BaseLab` 首页
- [ ] `08D-WBS-1.8.2-WBS-3.1实验迁移与入口解耦-研制计划.md`、`13-WBS-3.1-交互画布与渲染基座选型-研制计划.md` 与设计说明中，已明确写出本轮补入的动态连线与拖拽稳定性要求
