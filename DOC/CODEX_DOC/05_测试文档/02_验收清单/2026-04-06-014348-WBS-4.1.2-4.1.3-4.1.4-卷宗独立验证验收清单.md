# WBS 4.1.2 / 4.1.3 / 4.1.4 卷宗独立验证验收清单

## 验收范围
【你设置的验收项目已经通过，但是我觉得内容还缺很多】
1. `BaseLab` 首页中的 `WBS 4.1 卷宗独立验证`
2. `apps/base-lab/src/components/DossierAbilityLab.tsx`
3. `DOC/CODEX_DOC/03_研制计划/19-WBS-4.1.2-卷宗能力验证-研制计划.md`
4. `DOC/CODEX_DOC/03_研制计划/20-WBS-4.1.3-卷宗功能验证-研制计划.md`
5. `DOC/CODEX_DOC/03_研制计划/21-WBS-4.1.4-卷宗样式验证-研制计划.md`

## 人工验收项

- [√] `BaseLab` 首页能够看到并打开 `WBS 4.1 卷宗独立验证`
- [√] 进入页面后，左侧能看到 `主角相关` 筛选按钮，默认卷宗对象为 `苏玄`
- [√] 初始卷宗正文能够看到 `苏玄` 标题、摘要 `寒门出身的少年修士，正在青云宗外门求存。` 和底部 `审校队列`
- [√] `审校队列` 中能够看到 `新角色3`
- [√] 点击 `打开 青云宗 卷宗` 后，中央卷宗切换到 `青云宗`，并出现 `返回上一卷`
- [√] 点击 `返回上一卷` 后，中央卷宗返回 `苏玄`
- [√] 点击对象分类中的 `宗门` 后，卷宗会自动切换到 `青云宗`
- [√] 在右侧检查器将 `Summary` 改为 `已经在卷宗页改写` 后点击 `应用到本页样例`，中央卷宗摘要同步改写，顶部状态显示 `已应用到本页样例`
- [√] 常规桌面窗口下，`卷宗目录`、`卷宗正文`、`检查器`、`审校队列` 四块都能清晰识别且可滚动访问
- [√] `19 / 20 / 21` 三份研制计划文档的 `当前状态` 已更新为 `待人工验收`，并回链到本轮自测报告、验收清单与交接文档

## 补充人工验收项

- [ ] 在 `筛选关键词` 中输入 `林` 后，左侧列表仅保留 `林晚`，中央卷宗自动切换到 `林晚`
- [ ] 从 `苏玄` 点击 `打开 青云宗 卷宗` 后，`引用回查` 区域能看到 `回查 苏玄 卷宗`，点击后正文返回 `苏玄`
- [ ] 点击 `审校队列` 中的 `新角色3` 后，中央卷宗显示 `新角色3`、`待补录摘要`，且 `补录提示` 中出现 `待补录字段：Summary / Identity / Faction / Realm State`
- [ ] `检查器` 中 `Core Fields` 保留 `Id` 只读码值，`Additional Fields` 仍能独立看到 `Identity`、`Realm State` 等扩展字段

## 审阅层补充人工验收项

- [ ] 打开 `WBS 4.1 卷宗独立验证` 后，默认先进入 `设计说明`，并能看到 `卷宗设计思路`
- [ ] `设计说明` 中能直接看到 `为什么要做卷宗`、`页面清单`、`对象类型覆盖`、`典型链接关系`
- [ ] 点击 `样例规范` 后，页面出现 `样例数据格式规范`，并能直接看到 `objects/*.json` 与 `views/*.json` 的职责说明
- [ ] `样例规范` 中能直接看到 `fixtures/projects/sample-novel/manifest.json`、`fixtures/projects/sample-novel/objects/characters.json`、`fixtures/projects/sample-novel/views/saved-filters.json` 三个样例路径及对应 JSON 片段
- [ ] 从 `设计说明` 或 `样例规范` 切到 `工作面验证` 后，仍能进入原有卷宗目录、正文、检查器和审校队列联动页面

## 事实层补充人工验收项

- [ ] 在 `苏玄` 卷宗正文中能直接看到 `关系档案`，其中显示 `mentor`、`林晚` 与 `共同出场：试炼谷夺令`
- [ ] 在 `苏玄` 卷宗正文中能直接看到 `出场记录`，其中显示 `试炼谷夺令`、`章节切片：第一章 残火令现`
- [ ] 右侧检查器中存在 `Reference Fields` 分组，并能看到 `Faction` 之类的引用字段输入框及已解析引用按钮
- [ ] 将 `Faction` 清空并点击 `应用到本页样例` 后，正文中的 `打开 青云宗 卷宗` 跳转按钮消失，证明引用字段编辑已真正生效
- [ ] 在 `关系草拟` 区中选择 `char_new_003`、填写关系摘要后点击 `新建关系草案并打开卷宗`，页面会打开新的关系卷宗，并在正文中看到 `苏玄 ↔ 新角色3`
