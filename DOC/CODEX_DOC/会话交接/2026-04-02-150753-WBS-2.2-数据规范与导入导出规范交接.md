# WBS 2.2 数据规范与导入导出规范交接

## 本轮完成

1. 在 `packages/schema/src/index.ts` 中新增项目级 bundle 与对象级 bundle schema
2. 在 `packages/schema/src/index.ts` 中新增 schema 版本校验、bundle 版本校验、重复 ID 校验、跨对象引用校验
3. 在 `packages/schema/src/index.test.ts` 中新增基于 `fixtures/import-export/` 的正反样例测试
4. 在 `apps/service/src/lib/project-store.ts` 中新增项目导出、项目导入、对象批导出、对象批导入能力
5. 在 `apps/service/src/app.ts` 中新增四个 HTTP 接口：
   - `/api/projects/export`
   - `/api/projects/import`
   - `/api/projects/object-batch/export`
   - `/api/projects/object-batch/import`
6. 在 `apps/service/src/app.test.ts` 中新增项目往返一致性、对象批导出、对象批导入成功、对象批导入失败测试
7. 在 `fixtures/import-export/` 下新增项目级与对象级导入导出样例夹具
8. 根工作区 `typecheck / test / build` 已全部通过

## 当前状态

1. 当前活动链路：`WBS 0 -> WBS 2 -> WBS 2.2`
2. 当前节点状态：`待人工验收`
3. GitHub 远端状态目标：
   - issue `#3`：`OPEN`
   - Project `Status`：`In Progress`
   - issue 正文当前状态：`待人工验收`

## 下一步

1. 等待用户对 `WBS 2.2` 进行人工验收
2. 若用户确认通过，再将 issue `#3` 关闭并把 Project `Status` 切为 `Done`
3. 验收通过后，主线切换到 `WBS 3.1 知识库主视图增强`
