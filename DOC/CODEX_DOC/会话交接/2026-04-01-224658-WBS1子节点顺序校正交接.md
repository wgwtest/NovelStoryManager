# WBS 1 子节点顺序校正交接

## 本轮完成

1. 核对了 GitHub `#9` 的原生子 issue 顺序，确认此前确实是 `#16` 排在 `#15` 前面
2. 确认该顺序不承载业务含义，也不是 `Status` 或日期排序导致
3. 已通过 GitHub GraphQL `reprioritizeSubIssue` 将顺序校正为 `#1 -> #15 -> #16 -> #17 -> #18 -> #19`
4. 已确认 `#15` 和 `#16` 的 `Status`、`Work Type`、`Start Date`、`Target Date` 未被改动

## 待完成

1. 提交本轮证据文档并推送到 `origin/main`

## 关键入口

1. GitHub `#9 WBS 1 需求与方案`
2. GitHub Project：<https://github.com/users/wgwtest/projects/3>
