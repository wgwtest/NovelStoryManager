# WBS 1.7.2 原型稿执行说明

## 目标

在统一语义基线下，为当前主工作台产出 `4` 个核心工作面原型，用于承接最新评论后的页面结构重排和工作面深化。

## 当前结构收束

1. 左侧固定 `对象库`
2. 顶部主工作面固定为：
   - `卷宗`
   - `蓝图推演`
   - `剪辑编排`
   - `场景调度`
3. 右侧固定 `检查器`
4. 本轮不再把中区主结构理解为 `知识库 / 关系图 / 轨道` 三页切换

## 统一约束

1. 产品标题统一使用 `知识编辑器`
2. 项目名 `青云劫火录` 只作为次级上下文显示
3. 主工作台统一使用白色中性工作面，不使用暖黄底
4. 左列顺序统一为：搜索框、分类切换、统计动作、对象列表
5. 对象库统一覆盖九类对象：人物、势力、地点、物品、境界体系、事件、关系、线索、剧情弧
6. 所有标签、按钮、说明、区域标题统一中文化

## 四个工作面

1. `01_卷宗工作面`
   - 聚焦对象卷宗、摘要与审校动作
2. `02_蓝图推演工作面`
   - 聚焦依赖总线、推演节点与回路检查
3. `03_剪辑编排工作面`
   - 聚焦时间尺、片段篮与多层编排
4. `04_场景调度工作面`
   - 聚焦舞台图、地点调度与空间切换

## 每个目录的最少文件

1. `01-核心工作面.pen`
2. `01-核心工作面.png`
3. `方案说明.md`

## 生成与导出

1. 原稿生成命令：
   - `node scripts/generate-wbs-1.7.2-prototypes.mjs`
2. 导出命令：
   - `node scripts/export-wbs-1.7.2-prototypes.mjs`
3. 测试命令：
   - `node --test scripts/generate-wbs-1.7.2-prototypes.test.mjs scripts/export-wbs-1.7.2-prototypes.test.mjs`

## 当前执行结果

1. 当前有效目录固定为 `01_卷宗工作面` 到 `04_场景调度工作面`
2. 每个目录当前包含 `1` 个 `.pen`、`1` 个 `.png` 和 `1` 份 `方案说明.md`
3. 当前有效 PNG 导出图总数为 `4`
4. 旧的八套路线目录不再属于当前有效成果物

## 评阅重点

1. 是否一眼看出左侧对象库、中央工作面、右侧检查器的职责分工
2. 是否已经从旧三页切换结构收束为四个工作面结构
3. 四个工作面是否分别对应不同的工作角度，而不是同一版式换色
4. 每个目录是否只保留 `1` 张真正讲清主结构的核心图
