# WBS 6.2 多轨能力验证

## 基本信息

- `WBS` 编号：`WBS 6.2`
- 上级节点：`WBS 6`
- 当前状态：`待开发`

## 前置条件

- `WBS 6.1` 已完成多轨要素设计。

## 后续节点

- `WBS 6.3` 多轨功能验证

## 工作目标

- 先证明多轨视图具备真正基础能力，包括多轨显示、块入轨、块换轨、拖拽排序、时间锚点和视口操作。

## 工作内容

- 实现并验证多轨显示和分组切换。
- 实现并验证事件块入轨、换轨和排序。
- 实现并验证时间锚点、空间线、平移和缩放。

## 实施方式

- 每一项能力都要能通过界面观察确认，不再只给出静态结果。

## 成果物

- `apps/web/src/components/TracksView.tsx`
- `apps/web/src/lib/project-tracks.ts`
- `apps/web/src/App.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/src/styles.css`

## 时间安排

- 当前以 GitHub Project 中的 `Start Date` 与 `Target Date` 为准。

## 验收方法

- 查看对应自测报告，确认已覆盖前端测试和构建结果。
- 启动应用后，人工核验轨道分组、多轨展示、事件块入轨、换轨和时间锚点都可观察。
