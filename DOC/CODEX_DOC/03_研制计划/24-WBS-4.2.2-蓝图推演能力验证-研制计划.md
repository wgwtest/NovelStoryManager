# WBS 4.2.2 蓝图推演能力验证

## 基本信息

- `WBS` 编号：`WBS 4.2.2`
- 上级节点：`WBS 4.2`
- 当前状态：`待开发`

## 前置条件

- `WBS 4.2.1` 已完成蓝图推演要素设计。

## 后续节点

- `WBS 4.2.3` 蓝图推演功能验证

## 工作目标

- 先证明蓝图推演工作面具备真正的基础能力，包括创建节点、绘制连线、拖动画布、拖拽节点、显示坐标和属性编辑。

## 工作内容

- 实现并验证节点创建、节点选择、节点拖拽。
- 实现并验证边创建、边展示和边选中。
- 实现并验证画布大小配置、平移、缩放和坐标显示。
- 实现并验证节点属性编辑入口。

## 实施方式

- 每项能力都必须有明确的界面入口和可观察反馈。
- 不再使用“Graph tab 可看到节点”这类过于粗糙的验收语句。

## 成果物

- `apps/web/src/components/GraphView.tsx`
- `apps/web/src/lib/project-graph.ts`
- `apps/web/src/App.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/src/styles.css`

## 时间安排

- 当前以 GitHub Project 中的 `Start Date` 与 `Target Date` 为准。

## 验收方法

- 查看对应自测报告，确认已覆盖前端测试和构建结果。
- 启动应用后，人工核验能够创建节点、拖拽节点、创建连线、查看坐标、拖动画布并打开节点属性编辑入口。
