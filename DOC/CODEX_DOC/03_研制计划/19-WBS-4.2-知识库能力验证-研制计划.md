# WBS 4.2 知识库能力验证

## 基本信息

- `WBS` 编号：`WBS 4.2`
- 上级节点：`WBS 4`
- 当前状态：`待开发`

## 前置条件

- `WBS 4.1` 已完成知识库要素设计。

## 后续节点

- `WBS 4.3` 知识库功能验证

## 工作目标

- 先证明知识库工作台具备真正可观察的基础能力，例如对象筛选、类型切换、引用跳转、字段分组和对象编辑，而不是只展示一个静态页面。

## 工作内容

- 修正并验证对象筛选、对象自动切换和类型切换。
- 修正并验证引用点击后能切到目标类型并显示目标对象。
- 修正并验证检查器分组可见、可滚动、可操作。

## 实施方式

- 当前 `苏玄 -> 青云宗` 引用跳转问题和 `References` 区布局问题，在本节点优先解决。
- 验证项必须写成用户可观察动作，不使用抽象描述。

## 成果物

- `apps/web/src/App.tsx`
- `apps/web/src/components/ObjectLibrary.tsx`
- `apps/web/src/components/KnowledgeView.tsx`
- `apps/web/src/components/ObjectInspector.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/src/styles.css`

## 时间安排

- 当前以 GitHub Project 中的 `Start Date` 与 `Target Date` 为准。

## 验收方法

- 查看对应自测报告，确认已覆盖前端测试、类型检查和构建结果。
- 启动前端后，人工核验对象类型切换、关键字筛选、字段分组显示和引用跳转均可观察且行为明确。
