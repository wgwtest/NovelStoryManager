# WBS 1.5 技术方案选型

## 基本信息

- `WBS` 编号：`WBS 1.5`
- 上级节点：`WBS 1`
- 当前状态：`已验收`

## 前置条件

- `WBS 1.1` 产品边界确认与文档归档
- `WBS 1.2` 信息架构与页面分解
- `WBS 1.3` 低保真原型设计
- `WBS 1.4` 高风险交互原型验证

## 后续节点

- `WBS 1.6` 初始任务拆解与时间规划
- `WBS 2.1` 项目底座实施

## 工作目标

- 基于原型目标和交互验证结果，冻结进入 `WBS 2` 之前最关键的技术决定。

## 工作内容

- 冻结前端基础栈为 `React + Vite + TypeScript`。
- 冻结本地服务形态为 `Fastify + Node.js`。
- 冻结数据约束方式为 `TypeScript + Zod`。
- 冻结仓库组织为单仓库 `npm workspaces`。
- 冻结通用拖拽和分栏能力为 `dnd-kit` 与 `react-resizable-panels`。
- 冻结关系图方案为 `React Flow + elkjs`。
- 明确轨道视图首版采用 `dnd-kit + 自定义 DOM 轨道层`，不直接采用通用 timeline 插件。

## 实施方式

- 所有技术决定都必须直接回应既有原型目标和交互验证结论。
- 严格保持 AI 理解外置，`web` 不直写文件系统，`service` 不承载正文理解逻辑。

## 成果物

- [WBS-1.5-技术方案选型.md](/home/wgw/CodexProject/NovelStoryManager/DOC/CODEX_DOC/02_设计说明/WBS-1.5-技术方案选型.md)
- 本文件

## 时间安排

- 计划窗口：`2026-04-07` 到 `2026-04-08`

## 验收方法

- 人工核验方案正文中存在前端栈、本地服务、数据约束、拖拽与分栏、关系图、轨道视图、文件化项目结构、前后端职责划分和测试基线。
- 人工核验正文中明确写出“不在工具内部加入正文 AI 理解链路”和“不在首轮直接采用通用 timeline 插件”。
