# WBS 2.1 项目底座实施

## 基本信息

- `WBS` 编号：`WBS 2.1`
- 上级节点：`WBS 2`
- 当前状态：`已验收`

## 前置条件

- `WBS 1.5` 技术方案选型
- `WBS 1.6` 初始任务拆解与时间规划

## 后续节点

- `WBS 2.2` 数据规范与导入导出规范
- `WBS 3.1` 知识库主视图增强
- `WBS 4.1` 关系图工作台
- `WBS 5.1` 多轨编排视图
- `WBS 6.1` 观察输出层

## 工作目标

- 交付首个可运行的项目底座，包括文件化单项目存储、schema 包、本地服务、常驻对象库和知识库主视图。

## 工作内容

- 搭建 `npm workspaces` 工程骨架。
- 实现 `packages/schema`、`apps/service`、`apps/web` 的基础运行链路。
- 提供 `fixtures/projects/sample-novel` 示例项目，并在知识库视图中完成打开、查看和编辑。
- 保留 `Graph / Tracks` 入口，但不在本节点进入复杂交互实现。

## 实施方式

- 按“工作区骨架 -> schema 与示例项目 -> 本地服务 -> Web 壳层 -> 启动说明”的顺序落地。
- 保持对象事实和视图状态分文件存储。
- 历史细化计划已归档到 [03-WBS-L2-WBS-2.1-项目底座实施计划.md](/home/wgw/CodexProject/NovelStoryManager/DOC/CODEX_DOC/06_过程文档/02_历史计划/03-WBS-L2-WBS-2.1-项目底座实施计划.md)。

## 成果物

- `packages/schema/`
- `apps/service/`
- `apps/web/`
- `fixtures/projects/sample-novel/`
- 本文件

## 时间安排

- 计划窗口：`2026-04-09` 到 `2026-04-16`

## 验收方法

- 运行 `npm install`，预期依赖安装成功。
- 运行 `npm run typecheck`，预期退出码为 `0`。
- 运行 `npm run test`，预期退出码为 `0`。
- 启动 `npm run dev:service` 与 `npm run dev:web`，预期本地服务和前端页面均能启动。
- 人工打开示例项目，核验左侧对象库常驻、`Knowledge` 标签可查看并编辑集合数据、`Graph / Tracks` 标签仅显示占位入口。
