# WBS 4.4.2 场景调度能力验证

## 基本信息

- `WBS` 编号：`WBS 4.4.2`
- 上级节点：`WBS 4.4`
- 当前状态：`待开发`

## 前置条件

- `WBS 4.4.1` 已完成场景调度要素设计。

## 后续节点

- `WBS 4.4.3` 场景调度功能验证

## 工作目标

- 先证明场景调度工作面具备真正基础能力，包括地点节点展示、地点节点拖拽、场景切换线、驻留对象联动和冲突提示。

## 工作内容

- 实现并验证地点节点展示、选择和拖拽。
- 实现并验证切换线展示、选中和方向提示。
- 实现并验证驻留对象高亮、冲突提示和检查器联动。

## 实施方式

- 每一项能力都必须有明确的界面入口和可观察反馈。
- 不再使用“能看到地点列表”这类过于粗糙的验收语句。

## 成果物

- `apps/web/src/components/StageView.tsx`
- `apps/web/src/lib/project-stage.ts`
- `apps/web/src/App.tsx`
- `apps/web/src/App.test.tsx`
- `apps/web/src/styles.css`

## 时间安排

- 当前以 GitHub Project 中的 `Start Date` 与 `Target Date` 为准。

## 验收方法

- 查看对应自测报告，确认已覆盖前端测试和构建结果。
- 启动应用后，人工核验能够展示地点节点、拖拽地点节点、查看切换线、查看驻留对象高亮并收到空间冲突提示。
